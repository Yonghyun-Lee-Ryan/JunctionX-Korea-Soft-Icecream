---
type: 산출물
tags: [junction, 산출물]
created: 2026-08-04
updated: 2026-08-21
status: 빈칸
---

# 핵심기능

> [!important] 🔴 2026-08-21 — **이 파일도 배점 25점 문구가 이름으로 지목한 자리다**
> `Product Planning (25)` 원문 조각: `well-prioritized scope within the 3-day constraint` → 이 파일의 **티어**와 **「안 만드는 것」** 표 ([[08_요구사항_대장]] S4)
> ⚠️ **「Upstage 트랙의 배점이 확정됐다」는 사실이고, 「우리 트랙이 Upstage로 확정됐다」는 사실이 아니다.** 트랙 확정은 **8/21 22:20~22:25** D-2 대입, **8/22 00:00은 제출 마감**이다 → [[04_사전결정]] D-2

> [!important] 기능은 줄이는 쪽이 거의 항상 옳다
> 하나가 확실히 도는 게 셋이 반쯤 도는 것보다 항상 낫다. 특히 **Expo에서 3시간 동안 개발자들에게 보여야 한다면** 더 그렇다 — 그들은 **반쯤 도는 걸 즉시 알아본다** → [[02_동료해커]]

## 이 팀의 실현 가능 범위 — 기능을 정하기 전에 읽는다

개발자 2인이 **각자 28시간**([[02_팀과_제약]] 시간 예산 표의 개발 3행 합계 8+12+8 — 데모 준비 4시간은 뺀 값), 무박 후반 효율 감소를 반영하면 **유효 45 person-hours**다. **이 45가 기능을 만드는 전부다.**

> [!error] 🔴 기획자와 디자이너를 여기에 더하지 마라 — [[04_사전결정]] D-7 ✅확정
> D-7이 기획자를 **개발자 크리티컬 패스 밖**에 뒀다. 기여는 **데모 하네스**로 좁혀져 있다 — 시드 데이터 · 리셋 버튼 · 폴백 녹화본 · 목업 전환 스위치 · 데모 경로 스모크 러너. 디자이너는 피그마다.
> 둘 다 **기능을 만드는 시간이 아니라 개발자가 안 쓸 시간을 대신 쓰는 차선**이다. 45에 더하면 하드룰 2(28시간)를 **넘긴 걸 모르고 넘긴다.**
>
> D-7의 원래 취지가 여기 있다 — *"기능 하나 더 붙이는 게 최악이다."* 기획자가 프론트를 보조하면 코드는 늘지만 **막혔을 때 개발자를 부르고**, 그 인터럽트가 45를 깎는다. 차선으로 좁히면 안 부른다.

### AI가 몇 배를 만들어주나

개발자 둘 다 Claude Max·Codex Max를 풀로 돌린다. 에이전트 코딩은 **코드 작성**을 3~4배로 만든다. 그런데 개발 시간은 코드 작성만이 아니다.

```
코드 작성 50%  ·  디버깅·통합 35%  ·  환경·배포 15%
```

**AI는 앞의 50%만 줄인다.** 두 시스템이 안 붙는 이유를 찾는 시간, API가 예상과 다르게 응답하는 시간, 배포가 로컬과 다르게 도는 시간은 에이전트가 못 줄인다.

→ **전체 배속은 1.6~2배.** 4배가 아니다. 유효 45 × 1.8 ≈ **일반 팀 기준 80 person-hours어치**.
일반적인 강한 팀이 40~50을 뽑으니, **좋은 팀이 이틀 걸려 만들 걸 만들 수 있다** — 프로토타입이 아니라 **완결된 제품 한 조각**이다.

> [!note]- 위 분해는 측정값이 아니라 추정이다
> 그린필드 프로젝트의 통상적 비율로 잡았다. 팀의 실제 비율이 다르면 배속도 달라진다. **중요한 건 배수가 아니라 "AI가 못 줄이는 절반이 있다"는 구조다.**

### 🔴 45ph 계산에 없던 두 항목 — 파트너 제품을 쓰는 트랙이면 여기서 샌다 (2026-08-21)

위 45 person-hours는 08-04에 **우리 코드만** 놓고 잡았다. 브리핑이 파트너 제품 사용을 요구한 트랙이면 항목이 더 들어온다.

| 항목 | 45ph를 먹나 | 근거 |
| --- | --- | --- |
| **Studio 노드 조립** | 🟢 **거의 안 먹는다.** Chat이 워크플로를 대신 제안한다(덱 p.12) · *"build document automation agents with high accuracy in minutes — no code required"*(p.11) | 아래 「조립은 시간이 거의 안 든다」 콜아웃 |
| **노드 튜닝·재실행** | 🔴 **여기가 먹는다.** 스키마·프롬프트를 고칠 때마다 같은 문서를 다시 돌린다. 위 분해의 **디버깅·통합 35%** 쪽이고 **AI가 못 줄이는 절반**이다 | [[02_팀과_제약]] 크레딧 예산 함의 ① |
| **크레딧 소진** | ⛔ **person-hour가 아니다 — 별도 예산이다.** 팀 합계 `$50 in credits per participant` × 4명 = **$200**이 대회 중 전부 | 덱 p.29 → [[02_팀과_제약]] · [[09_되돌림_지점]] |

⚠️ **하드룰 2를 다시 읽는다** — *"28은 개발자 1인이 앉아 있는 시간이고, 2명이 병렬로 도는 만큼이 45ph다. 56ph로 읽으면 범위가 2배가 된다."* 🔴 **`28×2=56`은 하드룰 2가 명시적으로 금지한 산법이다.** 위 표는 **45ph 안에서 자리를 옮기는 것**이지 45를 늘리는 것이 아니다.
🔴 **크레딧은 시간이 아니라 데모 실패 지점이다.** 데모 중 소진은 `Service Completeness (20)`의 *"Does the demo run reliably end to end?"* 를 그 자리에서 깎는다 → [[04_데모시나리오]] 준비물

### 티어

| 티어 | 산출물 | 판단 |
| --- | --- | --- |
| **안전** (~40ph) | 배포된 URL · 핵심 플로우 **1개**가 끝까지 실제 동작 · 진짜 백엔드+DB(목업 아님) · **LLM API 1개 실연동**🔴 · 디자이너 손 탄 UI · 시드 데이터와 리셋 | 거의 확실히 됨 |
| **목표** (~70ph) | 위 + 핵심 플로우 **2~3개** + **기술적으로 어려운 것 1개**(RAG·에이전트 루프·**문서 파싱**🔴·실시간 스트리밍 중 하나) + 합성이 아닌 **실제 데이터**로 동작 + n을 밝힌 자체 측정 수치 1개 | **이 팀이면 현실적** |
| **무리** | 모바일 네이티브 · 실시간 협업 동기화 · 모델 학습 · 인프라 운영 · 로그인/회원가입 | 손대면 깨짐 → [[03_금지목록]] |

⚠️ **티어 이름은 안전 / 목표 / 무리 셋뿐이다.** 「도전」 같은 없는 이름을 만들지 마라 — [[01_심사기준]]이 이 표를 배점 문구에 매핑할 때 이름으로 찾는다.

> [!error] 🔴 2026-08-21 정정 — **🔴 표시 두 칸은 브리핑이 파트너 제품 사용을 요구한 트랙에서 뒤집힌다**
> 이 표는 08-04에 트랙 브리핑 **없이** 썼다. 20:00 Upstage 트랙 브리핑이 두 칸을 정면으로 건드린다.
>
> | 칸 | 무엇이 뒤집히나 | 근거 |
> | --- | --- | --- |
> | 목표 티어 **문서 파싱**🔴 | 🔴 **이 트랙에서 문서 파싱을 우리가 만드는 것은 즉사다.** 「기술적으로 어려운 것」으로 골랐다가 **필수 조건 위반과 배점 30 백지를 동시에** 낸다 | [[08_요구사항_대장]] N2 · [[03_금지목록]] *"Studio를 쓰는 것 = 필수 / Studio를 다시 만드는 것 = 즉사"* |
> | 안전 티어 **LLM API 1개 실연동**🔴 | 🔴 **필수 조건을 안 담고 있다.** M1은 *"\*Upstage Studio must power the core document-processing stages."*(p.27), M3·M4가 *"Combine Studio agents — Parse (structuring) · Classify (classification) · Extract (field extraction) · Instruct (summarization · translation · Q&A) — and integrate with any service via REST APIs & webhooks."*(p.27 · 한 문장 전체)를 요구한다. **배점 30이 `agent composition`을 직접 지목한다** | [[08_요구사항_대장]] M1·M3·M4·S1 |
>
> 🔴 **한 줄로**: **브리핑이 파트너 제품 사용을 요구한 트랙이면, 「기술적으로 어려운 것」은 그 제품을 다시 만드는 쪽이 아니라 그 제품 위에 얹는 쪽에서 고른다.** 안전 티어에는 **그 제품의 조립 결과가 끝까지 도는 것**이 들어간다.
> ⚠️ **위 표의 원래 문구를 지우지 않는다.** 나머지 4트랙(Microsoft·경상북도·POSTECH·labup)은 브리핑 자료가 없어 원래 예시가 그대로 유효하다. **「Upstage 트랙 내용이 확정된 것」이지 「우리 트랙이 Upstage로 확정된 것」이 아니다.**

> [!warning] 🔴 조립은 시간이 거의 안 든다 — **그래서 티어를 「노드 몇 개」로 짜면 틀린다** (Upstage 트랙 한정)
> 덱 p.12 Studio Chat 화면: 문서를 올리고 자연어로 시키면 Studio가 **워크플로를 스스로 제안한다** — *"I can build a Studio agent that **classifies each page** into those three categories (plus "Other") and then **extracts different fields per type** using separate extract schemas."*
> 🔴 **`in minutes — no code required`의 실체가 이것이다.** 조립 자체는 45ph를 거의 안 먹는다.
> 🔴 **그러면 배점 30의 `How deeply`는 조립 속도가 아니라 조립의 깊이·정확도·외부 연동에서 갈린다** — 배점 원문이 그 셋을 그대로 적었다: `agent composition, external integrations, and the difficulty and accuracy of the implementation`.
> → **티어에 「노드 N개를 엮는다」를 쓰지 마라.** *어떤 문서를 · 어떤 클래스로 쪼개서 · 무엇을 트리거하는가*와 **REST API·webhook으로 무엇에 붙였나**가 티어를 가른다 → [[08_요구사항_대장]] M4
> ⚠️ **근거는 슬라이드 이미지다(p.12).** PDF 추출 텍스트에는 안 잡힌다 → [[01_트랙_브리핑_원문]]

> [!error] 🔴 티어에 넣으면 8/22 새벽에 막히는 기능 둘 (Upstage 트랙 한정 · 근거는 슬라이드 이미지)
>
> | 넣지 말 것 | 화면에 떠 있는 것 | 어떻게 다룰 것인가 |
> | --- | --- | --- |
> | **「확신도가 낮으면 사람에게 넘긴다」식 human-in-the-loop 라우팅** | Classify 노드 설정 패널(p.22): *"Solar 모델은 confidence score 기능을 아직 제공하지 않습니다."* 모델 선택지는 `Auto` · `solar-pro2` | 🔴 **Solar 모델로는 지금 못 만든다.** 안전·목표 티어의 전제로 쓰지 마라. `Auto`가 무엇을 고르는지는 화면에 안 나온다 → 워크숍 확인 |
> | **Validate 노드**를 전제한 기능 | 툴바 스크린샷 3장(p.17·22·23)이 전부 `Parse` · `Classify` · `Extract` · `Instruct` 4개만 보여준다. 챌린지 문구(p.27)도 같은 4개를 나열한다. Validate는 **기능 소개 슬라이드(p.13)에만** 있다 | ⚠️ **「Validate 노드가 없다」고 단정하지 마라** — 베타 UI 스크린샷 3장이 근거일 뿐이다. **「조립 가능한 노드는 4개로 보인다」**로 적고 워크숍에서 확인한다 → [[05_워크숍_질문지]] |
>
> **둘 다 「검수」에 걸린다.** 사람 검수 화면을 우리가 처음부터 만들면 재현 쪽으로 기운다는 회색지대 경고가 이미 있다 → [[02_Upstage]] C절

**"기술적으로 어려운 것 1개"를 빼지 말 것.** 심사의 *기술적 도전*과 *독창성*이 거기서 갈리는데, AI 권장 대회라 API 호출 조합은 전 팀이 해온다.

🔴 **2026-08-21 — 이 문장의 두 단어가 배점 이름을 얻었다** (Upstage 트랙): *기술적 도전* = `Upstage Technology Implementation (30)`의 `the difficulty and accuracy of the implementation` · *독창성* = `Idea Creativity (25)`의 `Does the solution stand apart from existing services?` → [[01_심사기준]]

> [!error] 🔴 이 팀이 질 가장 큰 이유는 시간 부족이 아니다
> **코드가 싸지면 병목이 "무엇을 만들지"로 옮겨간다.** 만들기 쉬우니 기능을 더 넣고 싶어지고, 그러면 통합 지옥에 들어간다.
> 가장 그럴듯한 패배 시나리오는 **못 만들어서**가 아니라 **너무 많이 만들어서 아무것도 3시간 동안 안정적으로 안 도는 것**이다.
> → 아래 "최대 3개"는 AI 쓰는 팀에서 **느슨해지는 게 아니라 더 엄격해진다.** 그리고 코드베이스가 갈라지는 문제는 [[04_사전결정]] D-4에서 미리 막는다.

## 기능 목록

**최대 3개.** 4개째를 쓰고 싶으면 [[03_금지목록]]에 넣는다.

| # | 기능 | 어느 문제를 푸나 | 데모에 나오나 | 없으면 제품이 성립하나 |
| --- | --- | --- | --- | --- |
| 1 | `[채우기]` | → [[01_문제정의]]의 | ✅ | 아니오 |
| 2 | | | | |
| 3 | | | | |

**"어느 문제를 푸나"가 빈 기능은 잘라낸다.** [[04_일관성검사]]가 자동으로 잡아주는 항목이다.
**"없으면 성립하나"가 "예"인 기능도 잘라낸다.** 그건 있으면 좋은 것이지 핵심이 아니다.
**파트너 과제 문장의 어디에도 안 걸리는 기능은 잘라낸다.** 어느 문장에 걸리는지 못 대면 그건 우리가 만들고 싶은 것이다 → [[03_금지목록]] 「트랙 과제를 비틀어 하고 싶은 걸 하기」

🔴 **2026-08-21 — 브리핑이 파트너 제품 사용을 필수로 적은 트랙이면 기능마다 한 줄 더 적는다**: *"이 기능의 핵심 문서 처리 단계를 파트너 제품의 무엇이 돌리나."* 못 적는 기능이 하나라도 있으면 그건 감점 후보가 아니라 **필수 조건 위반 후보**다 → [[08_요구사항_대장]] M1
⚠️ **거꾸로도 잘라낸다.** 파트너 제품을 부르기만 하고 *"그래서 무슨 일이 끝나는가"* 가 없으면 `goes beyond "understanding" documents to actually "getting the work done."`(p.27)를 못 넘은 것이다 → [[08_요구사항_대장]] N1

## 근거 — 이 기능을 왜 만드나

> [!tip] 여기는 [[01_문제정의]]의 근거표와 **묻는 것이 다르다** — 같은 인용을 복붙하지 마라
> 문제정의는 *"이 문제가 진짜인가"* 를 묻는다. 여기는 *"왜 **이** 기능인가"* 를 묻는다.
> 문제가 진짜인 것과 이 기능이 그 문제의 답인 것은 **다른 주장**이고, [[03_회의론자]]는 둘을 따로 찌른다.

| 기능# | 등급 | 신뢰 | 인용 (그대로) | 누가 | 시각 | 이 말을 못 들었어도 만들었나 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | | | | | | |
| 2 | | | | | | |
| 3 | | | | | | |

🔴 **마지막 칸이 이 표의 전부다.** *"못 들었어도 만들었다"* 면 그 인용은 **근거가 아니라 장식**이다. 같은 브리핑을 읽은 60팀이 도착하는 자리라는 뜻이고, 인용을 붙여도 평범함은 안 없어진다 → [[11_평범한답]]
*"이 말을 듣고 방향을 바꿨다"* 가 나오는 기능이 **하나도 없으면 워크숍을 헛들은 것이다.** 22:00 전에 알아채야 고칠 시간이 있다.

🔴 **「누가」가 전부 `팀`·`우`면 그 근거는 우리끼리 만든 것이다.** 최소 한 줄은 `담`·`엔`이거나 **파트너 제품을 실제로 쓰는 사람**이어야 한다.

**등급** 🔴새 사실 / 🔴문서 뒤집기 / ⚠️문서와 모순 / 🔵회피 · **신뢰** ✅확실 / ⚠️재확인 · **누가** 담·엔·멘·스·팀·우 (모르면 `?`)
**앞 4칸**(등급·신뢰·인용·누가)이 [[10_발화채집]] A절 출력 그대로 — 옮겨 적지 말고 붙여넣는다. ⚠️ **채집 출력엔 `시각`이 없다** — `시각`과 맨 끝 칸은 손으로 채운다. 5칸을 통째로 붙이면 칸이 밀린다.

## 하나만 남긴다면

```
[채우기]
```

이걸 정해두면 시간이 모자랄 때(반드시 모자란다) 판단이 빠르다.

## 안 만드는 것 — 명시

심사에서 "이건 왜 없냐"에 답하기 위한 목록. **못 한 게 아니라 안 한 것**으로 만든다.

> [!important] 🔴 2026-08-21 — **이 표가 배점 25점 문구에 직접 걸린다** (Upstage 트랙 한정)
> `Product Planning (25)` 원문에 이 조각이 들어 있다: `well-prioritized scope within the 3-day constraint`.
> 🔴 **3일 제약이 변명이 아니라 채점 항목이다.** *"시간이 없어서 못 했다"*가 아니라 *"이 근거로 이 범위를 골랐고 나머지는 의도적으로 뺐다"*가 **점수가 된다.**
> [[08_요구사항_대장]]이 같은 말을 먼저 적었다 — *"⛔(의도적 제외) 표기가 감점 방어가 아니라 득점원이다."* **이 표의 줄 수가 곧 그 득점이다.**
> → **셋째 칸이 `(우리 판단)`이어도 줄 자체는 쓴다.** 위 티어 표의 **무리** 행과 「🔴 티어에 넣으면 8/22 새벽에 막히는 기능 둘」에서 그대로 옮겨온다.
> ⚠️ 배점 문구가 지목한 나머지 두 조각(`Concrete target users and problem definition` · `a screen & data-flow design…`)의 주인은 [[01_문제정의]]·[[02_타겟과_시나리오]]와 [[04_데모시나리오]]다 → [[08_요구사항_대장]] S4

| 안 만든 것 | 이유 | 근거 인용 — 파트너가 원하지 않는다고 **말한** 것 |
| --- | --- | --- |
| 로그인·회원가입 | 데모 점수에 기여하지 않고 시간을 가장 많이 먹는다 | (우리 판단) |
| `[채우기]` | | |

**셋째 칸이 채워진 줄이 가장 세다.** *"안 만들었습니다"* 보다 *"안 원한다고 하셔서 뺐습니다"* 가 훨씬 강하다 — 파트너 말을 들었다는 증거가 되고, 심사에서 「왜 없냐」를 **질문 자체로 성립 안 하게** 만든다. → [[06_트랙해부]] 「파트너가 원하지 않는 것」에서 옮겨온다

## 기술 메모

기획자가 채우는 게 아니라 **개발자에게 받아 적는 칸**. 이게 있어야 [[03_회의론자]]가 실현 가능성을 판정할 수 있다.

| 기능 | 어떻게 구현 | 예상 시간 | 위험 |
| --- | --- | --- | --- |
| 1 | | | |

## 관련

- [[JUNCTION-MOC]] · [[01_문제정의]] · [[04_데모시나리오]] · [[08_요구사항_대장]] · [[02_Upstage]]
