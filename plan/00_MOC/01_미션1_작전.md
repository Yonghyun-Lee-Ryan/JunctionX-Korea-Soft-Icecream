---
type: 산출물
tags: [junction, 작전, 미션1]
created: 2026-08-14
updated: 2026-08-21
status: draft
---

# 미션 1 작전 — 8/21 22:00~24:00

> [!success] ✅ 2026-08-21 — **트랙 확정: Upstage. 남은 건 미션 1 제출문과 마감이다**
> **트랙 결정은 끝났다** (시각 `[시각 기입]`) → [[04_사전결정]] D-2. 🔴 아래 표의 22:20~22:25는 **계획된 슬롯**이지 실제 결정 시각이 아니다.
> 🔴 **지금부터 이 문서의 목적은 하나다 — 아이디어 확정(23:00 컷오프) → 미션 1 제출문 집필 → 8/22 00:00 마감.**
> 트랙을 다시 논의하지 마라. 끝난 칸이다.
> **채점표는 [[01_심사기준]]의 Upstage 4항목 100점**이고, 그중 **기획자 소관이 50점**이다.

> [!note] 🔵 상태 (2026-08-21 밤)
> **20:00 Track Announcement 종료.** 확보한 트랙 자료는 **Upstage 1건**(30p 영문 전문 → [[01_트랙_브리핑_원문]]). 나머지 4트랙은 원문이 없다.
> **미션 1 마감 8/22 00:00은 그대로다.** 아래 타임박스는 유효하되 **입력이 5개에서 1개로 줄었다** — 바뀐 칸은 각 행에 표시했다.
> ~~🔴 **트랙은 아직 안 정해졌다.** 자료가 하나뿐인 것과 트랙이 하나로 정해진 것은 다르다~~ → ✅ **2026-08-21 확정 — 트랙 = Upstage.** ⚠️ **비교 없이 골랐다** — 나머지 4트랙은 원문이 없어 **판정 불가**였지 탈락이 아니다

> [!error] 이 2시간이 대회 전체를 정한다
> 트랙 발표 20:00–21:00, 파트너 워크숍 21:00–22:00. **우리끼리 정하는 시간은 22:00–24:00, 2시간뿐이다.**
> 미션 1은 채점 대상인지 미확정이지만, 그보다 중요한 게 있다 — **미션 1은 팀의 계약서다.** 여기 적힌 게 남은 46시간을 흔들리지 않게 만든다.
> 새벽 4시에 "근데 이거 왜 만들고 있지?"가 나오는 팀과 안 나오는 팀의 차이가 이 문서에서 갈린다.

**병목은 글쓰기가 아니라 결정이다.** 2023년 후기에 미션 1은 *"간략하게 써서 제출"*이었다. 문서를 다듬는 데 시간을 쓰면 안 된다.

## 타임박스

| 시각 | 구간 | 커맨드 | 누가 | 산출 |
| --- | --- | --- | --- | --- |
| ✅ 20:00–21:00 | 🔴 **요구사항 추출** — 브리핑 붙여넣고 ~~5트랙 전부~~ → **원문이 있는 트랙에만** 던지고 워크숍 간다. 실제 확보 = Upstage 1건 | `/jx-req` **×1** | **사람 시간 0분** | Upstage 대장 1장 (M·W·N·S·R) |
| 🔴 **22:00 즉시** | **크레딧 3단 등록 + 잔액 눈으로 확인** — Console 가입 → redeem `Edu_Junction_2026` → Studio 가입 | — | 🔴 **4명 전원이 각자.** 1인 5분, 넷이 동시 | 1인 $50 × 4 = **팀 $200** 확인 |
| 🔴 **22:00 즉시** | **T1 판정** — Studio를 하드룰 4의 「처음 쓰는 기술」로 세나 | — | **개발 2인. 30초, 한 번만** | 「센다 / 안 센다」 한 줄 |
| 🔴 22:00 | **발화 채집 분석** | `/jx-harvest` | 기획자. 3분 | 워크숍에서 들은 것 중 **문서에 없던 것** |
| ✅ 22:00–22:05 | 🔴 **1차 컷** — 대장의 **M·S만** 훑어 [[04_사전결정]] D-2 탈락 규칙에 대입 | — | 기획자. 5분 | ✅ **완료 — Upstage 탈락 규칙 ①🟢 ②✅ 통과.** 나머지 4트랙 원문은 **미확보 = 판정 불가** |
| 22:05–22:20 | **트랙 해부** + **평범한 답 선점** | `/jx-track` **+** `/jx-common` **동시** | 🔴 **원문 있는 트랙마다 1명 = 1명**(Upstage) / `/jx-common` 기획자 / **남는 2명 → 아래 콜아웃** | 트랙별 카드 + **금지 5줄** |
| ✅ 22:20–22:25 | **트랙 확정** | — | 기획자가 판정 규칙에 **대입** | ✅ **완료 — Upstage.** 확정 시각 `[시각 기입]` (이 슬롯 시각은 계획값이다) |
| 22:25 | 금지목록 반영 | — | `/jx-common`의 D를 [[03_금지목록]]에 붙임 | — |
| 22:25–22:50 | **아이디어 확산** | `/jx-diverge` | 4명이 **각자 다른 각도** | 후보 12개 |
| 22:50–23:00 | **수렴** | `/jx-skeptic` | 중복 제거 → 병렬 공격 | 후보 2~3개 |
| **23:00** | 🔴 **컷오프** | — | [[04_사전결정]] D-3의 결정권자 | **확정 1개** |
| 23:00–23:35 | 문서 작성 | 🔴 **`/jx-mission 1`** | **기획자 단독** (개발자는 이미 스캐폴드 착수) | 미션1 초안 |
| 23:35–23:50 | 제출 전 점검 | `/jx-consistency` | 게이트에 걸리면 여기서 되돌린다 | 수정본 |
| **23:50–00:00** | **제출** | — | 플랫폼 업로드 + 확인 | **제출 완료** |

> [!error] 🔴 2026-08-21 정정 — **`/jx-mission 1`이 이 표에 아예 없었고, 세 파일이 세 시각을 줬다**
> ~~`02_커맨드_사전`: 23:40~~ · ~~`JUNCTION-MOC`: 22:00에 미리 한 번~~ · 이 표: **0회**.
> 그런데 [[09_미션제출]] 스스로가 *"`/jx-mission`을 마감 30분 전에 처음 돌리면 게이트에 걸렸을 때 되돌릴 시간이 없다"*를 실패 모드로 적어놨다.
> → **집필 자체가 `/jx-mission 1`이다.** 23:00에 돌리고, 점검·제출에 25분을 남긴다. 23:40은 마감 20분 전이라 너무 늦다.
> ⚠️ **업로드 시간을 0으로 잡지 마라.** 마지막 10분은 파일 올리고 눌리는지 확인하는 시간이다.

> [!error] 🔴 2026-08-21 정정 — **마감이 있는데 이 표에 그 자리가 없었다** (크레딧 3단)
> [[08_요구사항_대장]] ②와 [[09_되돌림_지점]] ③이 redeem 등록·Studio 가입을 **「8/21 22:00 전 · 유일한 시각」**으로 못 박았다. 그런데 22:00~24:00 어느 행에도 그 항목이 없었다. **담당자와 소요시간이 없는 마감은 안 지켜진다.** 그래서 위 표에 두 행을 넣었다.
> **덱 p.30 원문**:
> `1/ Sign up for the Upstage Console: https://console.upstage.ai/home`
> `2/ Enter a redeem code: The redeem code and instructions are available on the Upstage Discord channel.  (Console - Dashboard - Credit - Redeem Code )` / `○ Redeem Code : Edu_Junction_2026`
> `3/ Join the Upstage Studio :  https://studio.upstage.ai/`
> 🔴 **3단이고, 「참가자 1인당」이다.** `$50 in credits per participant` — **4명이 각각 세 단을 밟아야 팀 $200**이 된다. 한 명이 대표로 하면 $50뿐이고, 그 순간 [[02_팀과_제약]] 크레딧 예산 표가 절반이 된다.
> 🟢 **Discord가 막혀도 코드는 이미 손에 있다** — 덱이 `Edu_Junction_2026`을 그대로 인쇄했다. Discord에만 있는 것은 **안내**지 코드가 아니다. Discord에서 막히면 안내 없이 `Console - Dashboard - Credit - Redeem Code`로 바로 넣어본다.
> ⚠️ [[02_팀과_제약]] B4의 *"Upstage 계정 ✅ 완료"*는 **가입까지**다. redeem 입력과 Studio 가입은 별건이다 — ✅를 보고 끝난 걸로 읽지 마라.
> 🔴 **잔액이 22:00에 안 찍혔으면 마감이 아니라 지연이다.** 다음 창구는 8/22 09:00 피드백이고 그 사이 **11시간은 Console 문의뿐**이다 → [[09_되돌림_지점]] ③.
> 🔴 **크레딧이 없으면 22:00~24:00에 범위를 못 정한다.** 그러면 이 표의 나머지가 종이가 된다.

> [!warning] 🔴 T1도 자리가 없었다 — **30초짜리인데 배정이 없으면 안 돈다**
> [[02_팀과_제약]]이 *"단정하지 않는다. 22:00 창구에서 개발 2인이 판정한다(한 번, 30초)"*고 적었는데 이 표에 그 30초가 없었다.
> **판정할 문장 하나**: Studio 워크플로 설계를 하드룰 4의 「처음 쓰는 기술」로 **세나.** 그 한 자리는 **브라우저 자동화**(데모 스모크 러너)가 이미 쓰고 있다.
> 「센다」로 나오면 ~~선택지는 둘 — ① Upstage 트랙을 D-2 탈락으로 보낸다 ②~~ → 🔴 **트랙이 Upstage로 확정돼 ①은 없어졌다. 남은 건 ② 하나다** — 브라우저 자동화 슬롯을 반납한다. ②는 [[04_사전결정]] D-6·D-7과 묶여 있어 **개발자 혼자 못 정한다.**
> 🔴 **후보마다 묻지 마라. 한 번만 묻는다** — 그 문답이 2시간을 먹는다.
> ⚠️ **이 판정은 바로 아래 22:00–22:05 1차 컷의 입력이다** — D-2 탈락 규칙 1번에 그대로 들어간다. 1차 컷이 시작되기 전에 끝나야 한다.

> [!warning] ✅ **완료 (2026-08-21)** · 🔴 1차 컷이 하는 일이 바뀌었다 — **거르는 게 아니라 대입이다**
> ✅ **결과: 트랙 = Upstage 확정.** ①은 대입 완료(①🟢 ②✅), ②는 **4트랙 원문 미확보로 판정 불가** — 아래 두 줄은 그때의 절차 기록이다.
> 원래는 대장 5장의 M·S를 비교해 후보를 **줄이는** 칸이었다. 지금은 대장이 1장이라 **비교할 대상이 없다.** 이 5분에 실제로 하는 건 둘 —
> ① **Upstage 대장의 M을 D-2 탈락 규칙 2개에 대입한다** — 「우리 스택 밖 기술이 2개 이상 필요한가」 / 「당사자를 이 건물 안에서 못 찾는가」
> ② **나머지 4트랙의 원문이 손에 들어왔는지 확인한다.** 들어왔으면 그 자리에서 `/jx-req`를 추가로 던진다(사람 시간 0분). 안 들어왔으면 그 넷은 **판정 불가 상태로 기록**한다 — 「자료가 없다」와 「탈락」은 다르다
> 🔴 **자료가 하나뿐이라는 이유만으로 트랙을 확정하지 마라.** 그건 판정이 아니라 체념이다. D-2에 대입한 결과로 적는다

> [!tip] 🔴 22:05 인력 — 해부 대상이 1개라 2명이 뜬다. 새 작전을 만들지 말고 **이 두 자리에 넣는다**
> ① **`/jx-common` 보강 1명** — 브리핑 Background가 예시를 **직접 네 개 적어줬다**: `scholarship applications · housing lease contracts · part-time employment contracts · club expense receipts`. **다른 팀도 같은 네 줄을 읽었다.** 이 넷은 「다른 팀이 낼 답」 1순위이고, 그대로 가면 **Idea Creativity 25점 직격**이다. `/jx-common` 출력의 A에 이 넷이 안 들어 있으면 손으로 넣는다 → [[03_금지목록]]
> ② **나머지 4트랙 자료 조달 1명** — 파트너 데스크·Discord·플랫폼 페이지. 구해오면 그 자리에서 `/jx-req`. 15분 안에 못 구하면 **접고 돌아온다**(시간 초과가 이 표를 깨는 가장 흔한 방식이다)

> [!warning] ✅ **지나간 칸이다 (트랙 확정 완료)** · 22:20–22:25가 5분인 게 이 표의 핵심이었다
> 🔴 **표기 통일 (2026-08-21)** — 다른 문서에 「트랙 확정 22:20」으로 적힌 곳이 있는데 그건 **구간 시작 시각**이다. 구간은 **22:20–22:25**다.
> 🔴 ~~확정 시각은 종료인 22:25로 읽는다~~ → **22:20·22:25는 계획된 슬롯이지 실제 결정 시각이 아니다.** 실제 확정 시각은 `[시각 기입]`이고, 아래 그래프의 `[트랙 확정] 22:25`도 계획값이다.
> 🔴 **8/22 00:00은 「미션 1 제출 마감」이지 트랙 결정 시각이 아니다.** 「자정에 정하면 되니 지금은 아이디어부터」가 나오면 [[04_사전결정]] D-2가 존재하는 이유가 통째로 없어진다.

> [!error] 🔴 2026-08-21 — **트랙이 5개다. 이 표를 그렇게 고쳤다**
> 파트너가 **Microsoft · Upstage · 경상북도 · POSTECH · labup** 다섯이다. 원래 표는 **3트랙 전제**로 *"개발2·디자인1이 트랙 나눠 병렬"*이라 적혀 있었는데, **4명이 20분에 5트랙을 해부할 수는 없다.**
>
> → **`/jx-req`를 먼저 쓴다.** 20:00에 브리핑을 붙여넣고 **5개 전부에 던진 뒤 워크숍에 들어간다** — 사람 시간이 0분이라 5개든 10개든 비용이 같다. 22:00에 돌아오면 대장 5장이 만들어져 있다.
> **22:00~22:05에 M(필수 조건)과 S(배점)만 훑는다.** 그것만으로 탈락 규칙이 대입돼 **5개가 2~3개로 준다.** 그 다음에 살아남은 것만 해부한다.
> 🔴 **전 트랙을 다 해부하려 하지 마라.** 그게 2시간을 먹는 가장 확실한 길이다.
> **트랙 선택을 토론하면 안 된다.** [[04_사전결정]] D-2에 판정 규칙을 미리 박아두면 **대입 5분**으로 끝나고, 안 박아두면 **1시간**이다.
> 그 문서를 D-6까지 채우는 이유가 이것 하나다.

> [!error] 🔴 2026-08-21 20:00 — **위 대비책의 전제가 다시 깨졌다. 이번엔 반대 방향이다**
> 위 콜아웃은 *"5트랙을 4명이 다 해부할 수 없으니 `/jx-req`로 병렬 처리해 5→2~3으로 줄인다"*는 계획이었다.
> 실제 발표에서 **우리 손에 들어온 트랙 자료는 Upstage 1건뿐이다.** 트랙이 5개인 건 그대로인데 **원문이 넷이 없다.**
> → **`/jx-req` ×5는 ×1이 됐다.** 병렬 fan-out이 5도 3도 아니고 **1**이다.
> → **1차 컷의 성격이 「거르기」에서 「대입 + 자료 확보 확인」으로 바뀌었다** (위 22:00–22:05 콜아웃).
> → 🔴 **여기서 절약된 15분을 아이디어 확산(22:25–22:50)에 얹지 마라.** 확산은 25분으로 이미 설계된 칸이고, 늘리면 수렴이 밀린다. 절약분은 **22:05 두 자리**에 쓴다

## 그래프

```
      [트랙 브리핑 원문]  ← 20:00 확보, 요약 금지
             │
   ┌─────────┼─────────┐        ← fan-out: 트랙별 병렬 해부
   ▼         ▼         ▼
  T1        T2        T3         /jx-track
   └─────────┼─────────┘        ← fan-in: 판정 규칙 대입 (토론 아님)
             ▼
        [트랙 확정] 22:25
             │
   ┌────┬────┼────┬────┐        ← fan-out: 4명 × 서로 다른 각도
   ▼    ▼    ▼    ▼
  A①   A②   A③   A④            /jx-diverge
   └────┴────┼────┴────┘        ← fan-in: 중복 제거
             ▼
        [후보 3~4개]
             │
   ┌─────────┼─────────┐        ← fan-out: 후보마다 동시 공격
   ▼         ▼         ▼
  죽임      죽임      생존        /jx-skeptic
             ▼
        [확정 1개] ★23:00
```

🔴 **2026-08-21 실제** — 첫 단 fan-out은 *원문이 있는 트랙 수*만큼 열린다. 지금은 **폭 1(Upstage)**. 🟢 **아래 두 단은 그대로 열린다** — 확산 4각도, 회의론자는 후보마다. 위가 좁아졌다고 아래를 좁히면 이 문서가 없어도 되는 상태가 된다.

### 왜 이렇게 생겼나 — 설계 근거 셋

**① 다양성은 사람 수가 아니라 각도 배정에서 나온다.** 4명이 같은 프롬프트를 돌리면 거의 같은 답 4개가 나온다. 출발점을 강제로 갈라야 구조적으로 다른 답이 나온다 → [[07_아이디어확산]]

**② 회의론자는 순차가 아니라 병렬로 건다.** 후보를 하나씩 검토하면 앞의 것에 앵커링된다. 동시에 걸어 각각 독립적으로 죽인다.

**③ 상태는 대화가 아니라 파일에 둔다.** 2시간 동안 대화창이 길어지면 초반 지시가 희석된다. 커맨드가 매번 `00_고정` 4개를 다시 읽는 이유다.

## 미션 1 문서 — 작성 프롬프트

23:00 이후 기획자가 단독으로 쓴다. `20_산출물/01_문제정의`와 `03_핵심기능`을 채우는 것이 곧 제출물이다.

```
아래 확정 사항으로 해커톤 미션 1 제출 문서를 써라.
[확정 트랙] [확정 아이디어] [회의론자 지적과 우리 답] [00_고정 4개]

## 규칙
- 분량을 늘리지 마라. 읽는 사람은 60팀 것을 본다.
- 형용사 금지. [금지목록]의 표현 금지를 전부 지켜라.
- 파트너 브리핑의 단어를 그대로 써라. 동의어로 바꾸지 마라.
- 안 만든 걸 만든 것처럼 쓰지 마라. 미래형 가능성 표현 금지.
- 🔴 문제의 주인은 **파트너 기업**이다. 개인의 불편이 아니라 파트너가 **돈·시간·리스크**를 걸고 있는 과제를 쓴다. 부딪치면 파트너 쪽.
- 🔴 2026-08-21 — ~~Upstage 트랙으로 확정될 경우~~ → ✅ **확정됐다. 위 줄은 실제로 반쯤 뒤집혔다.** 브리핑 원문:
  "Any domain is welcome — problems students genuinely relate to are especially encouraged."
  이 트랙에서는 **참가자 본인이 당사자여도 감점이 아니다.** 대신
  "*Upstage Studio must power the core document-processing stages."
  가 **필수 조건(Upstage Technology Implementation 30점)**이므로, 구조 3에 Studio 노드가
  어디에 들어가는지가 반드시 있어야 한다. ~~**다른 트랙으로 가면 위 줄이 그대로 유효하다.**~~ → ✅ **다른 트랙은 없다.**
- 🔴 인용의 화자는 [현장메모]에 적힌 사람뿐이다. 가상 인물·가상 발화를 지어내지 마라.

## 구조
1. 한 문장 — [누가]는 [상황]에서 [무엇] 때문에 [손해]를 본다. **[누가]는 파트너 조직 안의 사람이고, [손해]는 파트너의 돈·시간·리스크 중 하나로 쓴다**
   ✅ **트랙 = Upstage 확정(2026-08-21)이므로 [누가]에 학생·참가자 본인이 들어가도 된다**(위 규칙 단서).
   Product Planning 25점이 요구하는 건 "Concrete target users and problem definition" —
   **파트너 소속 여부가 아니라 구체성**이다.
2. 왜 이 트랙인가 — 파트너 브리핑 인용 1개 + **워크숍에서 담당자가 한 말이 있으면 1개 더** + 우리가 답하는 부분
   🔴 **인용은 영문 원문 그대로.** 번역하면 심사자가 자기 문서의 단어를 못 알아본다.
   원문은 [트랙 브리핑 원문].
3. 무엇을 만드는가 — 기능 최대 3개, 각각 **파트너의** 어느 과제를 푸는지. 유저플로우는 그 과제가 **누구 손에서** 처리되는지로 쓴다
4. 48시간에 되는 근거 — 팀 스택과 연결
5. 범위 밖 — **파트너가 원하지 않는다고 말한 것을 맨 위에**, 나머지는 못 한 게 아니라 안 한 것으로
```

---

## ✅ 구조 4번 「48시간에 되는 근거」 — **미리 써뒀다** (2026-08-21)

> [!success] 이 문단만 트랙과 무관하다
> 미션 1의 다섯 구조 중 1·2·3·5는 트랙을 알아야 쓴다. **4번은 팀 구성과 스택 이야기라 지금 쓸 수 있다.**
> 2026-08-18 리허설에서 이 문단이 **통째로 비어 나왔다** — 스택이 미정이라 프레임워크·배포·역할 분담을 한 글자도 못 썼고 나온 건 API 이름 두 개뿐이었다. 지금은 스택이 확정돼 채울 수 있다.
> 🔴 **23:00~23:40 집필 40분에서 5~10분을 번다.** 그대로 붙여넣고 트랙 이름만 맞추면 된다.

### 짧은 판 — 분량 제한이 빡빡할 때 (약 180자)

```
기획 1명, 개발 2명, 디자인 1명이다. 프론트는 플러터, 백엔드는 노드, DB는 포스트그레로
트랙 발표 전에 정했고 바꾸지 않는다. 저장소 하나를 front와 backend로 나눠 개발 2명이
각각 맡고, 둘이 만나는 지점인 API 스키마는 8월 22일 4시에 고정한다. 개발에 28시간을
쓰고, 8월 23일 8시부터는 기능을 붙이지 않고 데모만 반복한다.
```

### 긴 판 — 제한이 없거나 넉넉할 때 (약 400자)

```
기획 1명, 개발 2명, 디자인 1명으로 진행한다.

프론트엔드는 플러터, 백엔드는 노드, 데이터베이스는 포스트그레로 쓴다. 트랙 발표 전에
이 구성으로 정했고 대회 중에 바꾸지 않는다.

저장소는 하나를 쓰고 front와 backend로 나눈다. 개발 2명이 각각 맡는다. 두 사람이
만나는 지점은 API 스키마 하나뿐이라, 8월 22일 4시에 스키마를 고정하고 그 뒤로는 두
사람이 함께가 아니면 바꾸지 않는다.

8월 22일 0시부터 4시까지는 두 사람이 같은 코드를 만든다. 프로젝트 뼈대와 데이터 모델,
API 스키마를 이 시간에 만들고, 4시에 배포해서 화면이 뜨는 것을 확인한 뒤 갈라진다.

개발에 쓰는 시간은 28시간이다. 8월 23일 8시부터 12시까지는 데모를 반복해서 돌려보는
데 쓰고, 그때부터는 기능을 붙이지 않는다. 잠은 교대로 잔다.

기획은 문제 정의와 발표를 맡고 개발 경로에는 들어가지 않는다. 대신 시연용 데이터와
초기화 버튼, 녹화본을 맡아 부스에서 매번 같은 화면이 나오게 한다.
```

### 영어판 — 폼이 영어일 때 (약 450자)

```
We are four: one planner, two developers, one designer.

We fixed the stack before the tracks were announced and do not change it during the
event: Flutter on the front, Node on the back, PostgreSQL for data.

We use one repository, split into front and backend, one developer each. The only
place the two meet is the API schema. We freeze that schema at 04:00 on August 22 and
after that neither person changes it alone.

From 00:00 to 04:00 on August 22 both developers write the same code: the project
skeleton, the data model, and the API schema. At 04:00 we deploy and confirm a screen
loads, then we split.

We spend 28 hours building. From 08:00 to 12:00 on August 23 we rehearse the demo, and
from that point we add no features. We sleep in shifts.

The planner owns the problem statement and the pitch, and stays off the build path.
Instead the planner owns the demo data, the reset button, and a recorded fallback, so
the booth shows the same screen every time.
```

> [!warning] 붙여넣은 뒤 확인할 것 ~~셋~~ → **다섯** (🔴 08-21 트랙 발표로 둘 늘었다)
> ① **트랙 이름을 넣었나** — 이 문단에는 일부러 안 넣었다
> ② **본문 맨 끝에 「아직 만든 것은 없다」를 넣었나** — 미션 1은 만든 게 0개인 시점이라 평서형 현재만 쓰면 이미 도는 것처럼 읽힌다
> ③ **분량** — 이 문단이 전체의 3분의 1을 넘으면 짧은 판으로 바꾼다. 읽는 사람은 60팀 것을 본다
> ④ 🔴 ~~Upstage 트랙이면~~ → ✅ **트랙 확정(2026-08-21). Studio가 이 문단에 없다 — 지금 넣어야 한다** — 위 세 판은 트랙 발표 전에 쓴 거라 스택이 플러터·노드·포스트그레뿐이다. 브리핑의 `*Upstage Studio must power the core document-processing stages.`는 **필수 조건**이고 **Upstage Technology Implementation 30점**이 여기 걸린다. **한 줄을 넣는다** — 어느 노드(`Parse` · `Classify` · `Extract` · `Instruct`)를 어디에 쓰는지. 🔴 **아직 안 정했으면 지어내지 말고 비워둔다**
> 　　🔴 **④의 노드 목록에서 `Validate`를 뺐다 (2026-08-21 정정).** 덱 p.13 기능 소개에는 5번째 노드로 있지만, **Studio 화면의 단계 추가 툴바(p.17·22·23)에도 Use of Technology 문구(p.27)에도 없다.** 브리핑이 스스로 나열한 건 넷이다 — *"Combine Studio agents — Parse (structuring) · Classify (classification) · Extract (field extraction) · Instruct (summarization · translation · Q&A)…"*. **없다고 단정도 하지 마라** — 근거가 베타 UI 스크린샷 3장뿐이다. 화면에서 확인한 뒤에 쓴다
> ⑤ 🔴 **크레딧이 새 제약이다** — Benefits는 `$50 in credits per participant`. 4명이면 $200이다. 개발 중 반복 호출이 이걸 먹고, **크레딧 소진은 8/23 데모가 죽는 방식 중 하나다** → [[09_되돌림_지점]]. 미션 1 문서에 쓸 필요는 없지만 **범위를 정할 때 알고 있어야 한다**

🔴 **배포처(A5)가 정해지면 한 줄 추가한다** — 긴 판·영어판의 "4시에 배포해서 화면이 뜨는 것을 확인한 뒤" 문장에 어디에 배포하는지를 넣는다. 지금은 미정이라 비워뒀다.

## 🔴 하지 말 것 ~~셋~~ → **넷** (🔴 08-21에 ④가 늘었다)

**① 현장에서 자동화를 손보지 마라.** 커맨드는 **대회 전에** 완성해 간다. 22:00에 커맨드가 안 돌면 고치지 말고 **`30_역할/`의 md를 복붙**한다. 그러라고 프롬프트 원문을 md에 남겨뒀다.

**② 호출을 많이 하지 마라.** 2시간에 사람당 **3~4회가 상한**이다. 상한은 API가 아니라 **사람의 독해 속도**다. 안 읽을 거면 안 돌리는 게 낫다.

**③ "아이디어 10개 줘"를 하지 마라.** 그럴듯한 평균값이 10개 나오고 고를 근거가 없어 오히려 더 오래 걸린다. 각도를 지정한 3개가 무각도 10개보다 낫다.

**④ 🔴 브리핑이 예로 든 문서 넷을 그대로 아이디어로 쓰지 마라.** `scholarship applications · housing lease contracts · part-time employment contracts · club expense receipts` — 이건 **친절이 아니라 함정에 가깝다.** 같은 줄을 다른 팀도 읽었고, 그대로 가면 *"Does the solution stand apart from existing services?"*(Idea Creativity 25)에서 첫 문장부터 깎인다. 22:05 `/jx-common`으로 **선점해서 [[03_금지목록]]에 넣는다.** 🔴 단 [[11_평범한답]]의 C(그래도 맞을 수 있는 것)를 확인할 것 — 넷을 전부 금지하면 가장 옳은 답까지 버린다.

## 대회 전 리허설 — 이게 자동화보다 중요하다 — ⏹ **2026-08-21 창구 종료**

> [!error] 🔴 2026-08-18 — 리허설 일정이 없다. **8/20(목) 회의가 곳 리허설이다**
> 전원 직장인이라 모이는 날은 8/20 하루뿐이다. 회의 90분 + 리허설 120분은 퇴근 후에 안 들어간다.
> → **리허설을 회의로 삼는다.** 아래 22:00–23:00 구간을 1:1로 돌리면서 막힐 때마다 멈춰 [[04_사전결정]]을 박는다 → **[[05_최종회의_안건]]**

**재료는 준비돼 있다 → [[99_리허설용_가짜_트랙브리핑]]** (트랙 3개, 영문, 판정 규칙을 시험하도록 설계).
팀 4명이 모여 **이 2시간을 통째로 실제로 돌려본다.** 리허설이 잡아주는 것:

⏹ **2026-08-21 — 대회가 시작돼 이 창구는 닫혔다.** 아래 다섯 항목은 지운 게 아니라 **마감 시점 상태**다. 결과 기록은 [[06_리허설결과_20260819]].
🔴 특히 *「23:00 컷오프에서 정말로 결정이 되는지」*는 **08-19 리허설이 실제로 쟀고, 결과가 「생존 0개」였다** — 못 고른 게 아니라 **고를 것이 없었다.** 오늘 밤 이 칸에서 같은 일이 나면 그 문서의 후보안(치명 2개 이상이면 죽인다 / 전멸하면 치명이 가장 적은 것을 살린다)을 그대로 대입한다. 🔴 **다만 리허설은 에이전트가 돌렸고 사람 4명의 시간은 여전히 미측정이다.**

- [ ] 프롬프트가 쓸모 있는 출력을 내는지
- [ ] **2시간 배분이 맞는지** — 위 표는 계산일 뿐 검증된 게 아니다
- [ ] 4명이 병렬로 돌렸을 때 결과를 합치는 데 실제로 몇 분 걸리는지
- [ ] **23:00 컷오프에서 정말로 결정이 되는지** ← 여기가 제일 안 될 것 같다
- [ ] 커맨드가 4명 환경에서 똑같이 도는지

## 관련

- [[JUNCTION-MOC]] · [[04_사전결정]] · [[06_트랙해부]] · [[07_아이디어확산]] · [[03_회의론자]]
