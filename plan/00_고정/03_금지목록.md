---
type: 고정컨텍스트
tags: [junction, 금지목록]
created: 2026-08-04
updated: 2026-08-21
status: 상시
---

# 금지목록

> [!note] 왜 이 문서가 지시문보다 강한가
> **"이런 걸 해라"보다 "이런 건 안 된다"가 출력을 훨씬 잘 좁힌다.** 그리고 버린 아이디어를 기록하지 않으면 모델은 **같은 걸 또 제안한다** — 대화창을 새로 열 때마다 기억이 없으니 당연하다.
> 대회 중 **가장 자주 갱신되는 파일**이 이것이어야 한다. 뭔가를 버릴 때마다 30초만 써서 여기 적는다.

## 쓰는 법

아이디어를 버릴 때마다 한 줄. **버린 이유를 반드시 같이 적는다** — 이유가 없으면 나중에 "그거 왜 안 됐지?"로 되돌아온다.

```
| 버린 것 | 언제 | 왜 버렸나 |
```

## 버린 아이디어

| 버린 것 | 언제 | 왜 버렸나 |
| --- | --- | --- |
| `[예시] 실시간 협업 화이트보드` | D1 14:00 | WebRTC를 아무도 안 해봄. 48시간 내 불가 |
| | | |
| | | |

## 처음부터 제외 — 해커톤 기획의 상시 금지

대회와 무관하게 항상 적용되는 것들. 모델이 자주 흘러가는 방향이라 미리 막는다.

| 금지 | 이유 |
| --- | --- |
| **"~를 위한 플랫폼"** | 플랫폼은 양면 시장이라 48시간에 한쪽도 못 채운다 |
| **사용자 수·시장 규모 추정치를 지어내기** | 심사 질의응답에서 가장 먼저 깨진다. 출처 없는 숫자는 아예 쓰지 않는다 |
| **"AI로 ~를 해결"만 있고 어떻게가 없는 기획** | 심사위원이 매년 수십 개를 본다. 차별점이 0 |
| **로그인·회원가입·관리자 페이지 만들기** | 데모에서 점수가 안 되는데 시간을 가장 많이 먹는다 |
| **완성도 낮은 기능 여러 개** | 하나가 확실히 도는 게 셋이 반쯤 도는 것보다 항상 낫다 |
| **트랙 과제를 비틀어 하고 싶은 걸 하기** | Challenge Partner는 **자기 과제의 답**을 보러 왔다 |

| **팀이 배우고 싶은 기술·주제를 트랙 선택 기준에 넣기** | 트랙은 파트너의 과제로 고른다. 학습 욕구는 스택 선택([[02_팀과_제약]] A)에서만 쓴다 |
| **발표 앞부분을 배경 설명으로 쓰기** | 3분 중 2분을 배경에 쓰면 정작 만든 걸 못 보여준다 |

## 이번 대회 한정 금지 — JunctionX Korea 2026

### 🔴 파트너가 이미 파는 것 — 만들면 즉사 (2026-08-21 `/jx-biz` 실측)

| 파트너 | 이미 있는 것 | 만들면 |
| --- | --- | --- |
| **Microsoft** | Foundry Agent Service · Agent Framework · Copilot Studio · Agent 365 · Entra Agent ID · Foundry IQ(RAG) — **에이전트 스택의 거의 모든 층이 GA다** | 🔴 **또 하나의 에이전트 빌더·오케스트레이터.** 3년간 저작 층을 **여섯 번** 자기 손으로 갈아엎었다(PVA→Bot Framework→SK/AutoGen→Assistants API는 **8/26 은퇴**) |
| **Upstage** | 🟢 **덱 확인분(2026-08-21)**: Studio Automate 노드 5개 — **Parse · Classify · Extract · Validate · Instruct** · Studio Chat(Search/RAG · Document Chat · Document Translation) · Document Parse(2024-10) · Information Extract(2025-05) · Agents(2026-01) | ⚠️ **행 전체가 다시 쓰였다 — 아래 정정 콜아웃과 구분 표를 반드시 같이 읽을 것** |
| **경상북도** | 🔴 **「2026 주민체감형 AI 전환 경진대회」 — 7/15~9/15 공모 중** · **모이소**(공공 마이데이터 앱, 농어민수당 71만 건) · 청년e끌림 · 투어054 | 🔴 **도가 같은 주제를 전국민 대상으로 지금 공모 중이다.** 그리고 부산이 「전국 최초 생성형 AI 행정서비스」를 이미 가져갔다 |

| **labup = 래블업** | 🔴 **AI:GO**(개인 PC에서 클라우드 없이 LLM 구동 — **2026년 1월 CES 출시**) · **AI:DOL**(브라우저 모델 플레이그라운드, 같은 날 출시) · Backend.AI(GPU 가상화·스케줄링) · FastTrack(ML 파이프라인) · all-smi(GPU 모니터링) | 🔴 **「로컬에서 LLM 돌리기」와 「브라우저 모델 플레이그라운드」는 8개월 전 신제품이다.** 여러 팀이 자연스럽게 흘러갈 방향이라 특히 위험하다 |

| **POSTECH** | 🔴 **Apple 디벨로퍼 아카데미 @ POSTECH**(연 200명, 9개월, 챌린지 기반 학습) · **AIR센터**(성과분석·정책개발 전담) · 창업경진대회 14회·GSSC·Star-dium·이노폴리스캠퍼스 · 포스텍홀딩스 · 체인지업그라운드 | 🔴 **아이디어를 창업으로 잇는 조직·건물·투자사·프로그램을 이미 전부 갖고 있다.** 「도구가 부족하다」고 가정하면 그중 하나와 겹쳐 끝난다. **KAIST 창업원도 이 대회에 트랙을 낸 전례가 있다** |

**출처 없는 숫자는 안 썼다.** 상세 근거는 `/jx-biz` 카드에 있다.

> [!error] 🔴 2026-08-21 정정 — **Upstage 행은 반쯤 뒤집혔다. Studio는 「쓰면 죽는 것」이 아니라 「안 쓰면 죽는 것」이다**
> 08-21 `/jx-biz` 실측으로 이 표는 *"문서 넣고 → 구조화 → 사람 검수 → 인용 달아 답하기는 통째로 이미 제품이다"* 라고 적었다. 트랙 브리핑이 정반대를 요구한다.
> **원문 (덱 p.27)**: *"Combine Studio agents — Parse (structuring) · Classify (classification) · Extract (field extraction) · Instruct (summarization · translation · Q&A) — and integrate with any service via REST APIs & webhooks. **\*Upstage Studio must power the core document-processing stages.**"*
> **원문 (덱 p.28, 배점 최대)**: *"Upstage Technology Implementation (30) — How deeply the team leveraged Upstage Studio and its APIs — **agent composition, external integrations, and the difficulty and accuracy of the implementation.**"*
> 🔴 **2026-08-21 2차 정정 — 이 인용이 잘려 있었다.** *"…and its APIs."* 에서 마침표를 찍어 끝냈는데, 잘린 뒷절이 하필 **`agent composition`** — 30점을 실제로 가르는 절이다. 「Studio를 썼다」가 아니라 **「노드를 어떻게 엮었나 · 밖으로 무엇과 붙였나 · 얼마나 어렵고 정확한가」**가 배점이다. 전문으로 복원했다. 원문 블록은 [[01_심사기준]]·[[01_트랙_브리핑_원문]]
>
> | 하는 짓 | 판정 |
> | --- | --- |
> | Studio 노드(Parse·Classify·Extract·Validate·Instruct)를 엮어 **쓴다** | ✅ **필수.** 안 쓰면 요건 위반이고 배점 30을 백지로 낸다 |
> | Studio가 하는 일을 **직접 구현한다** — 자체 파서·추출기·분류기·OCR 파이프라인 | 🔴 **즉사 ×2.** 이미 제품인 걸 다시 만들면서 30점까지 스스로 버린다 |
> | 문서 Q&A·인용 하이라이트 뷰어 **그 자체**를 제품으로 낸다 | 🔴 **즉사.** 08-21에 적은 원래 경고가 유효한 자리는 정확히 여기다 |
> | 「노코드 문서 워크플로 빌더」를 만든다 | 🔴 **즉사.** Studio가 그 제품이다 |
> | Studio 위에 **「그래서 무슨 일이 끝나는가」**를 얹는다 | ✅ **이게 과제다** — *"goes beyond 'understanding' documents to actually 'getting the work done.'"* |
>
> 🔴 **한 줄로**: **Studio를 쓰는 것 = 필수 / Studio를 다시 만드는 것 = 즉사.** 「이해」에서 멈추면 그것도 감점이다.
> ⚠️ **이 판정은 Upstage 트랙에 한한 것이다.** 다른 트랙으로 가면 Studio를 쓸 의무가 없다 → [[01_심사기준]]

> [!note]- 🔴 덱에 없는 것 — 단정 금지
> 이 표가 08-21에 적은 **AI Space(문서 Q&A+인용 하이라이트) · File Search · 「Studio 2026-04 출시」**는 **덱에 없다.** 덱 제품 타임라인은 Solar Mini(2024-01) → Solar Pro(2024-09) → Document Parse(2024-10) → Information Extract(2025-05) → Solar Pro 2(2025-07) → **Agents(2026-01)**까지다.
> **덱에 없다고 제품이 없어진 건 아니다.** 지우지 않고 *"덱에는 없다"*로만 표기하고 **워크숍 확인 항목**으로 넘긴다 → [[05_워크숍_질문지]]
> ⚠️ 그동안 이 표에 **Validate·Instruct 두 노드가 빠져 있었다.** 특히 **Validate(추출 정확도 검증)** 는 「사람 검수」를 자동화하는 노드라 기획에 직결된다.
>
> 🔴 **2026-08-21 2차 — 그렇다고 Validate를 전제로 기획하지 마라.** 위 판정표는 노드 다섯을 엮으라고 적었지만, **실제 화면에서 조립 가능한 노드는 넷으로 보인다.** Validate는 **p.13 기능 슬라이드에만** 있고 — Challenge Statement의 `Combine Studio agents`에도, **p.17·22·23 세 화면의 `Add Step` 툴바에도 없다**(전부 `Parse · Classify · Extract · Instruct`).
> ⚠️ **「Validate 노드가 없다」고 단정하지도 마라.** 근거는 스크린샷 3장이고 베타 UI일 수 있다(도메인이 `studio.beta.devstage.ai`다).
> → **Validate 없이 성립하는 설계로 간다.** 있는 걸 안 쓰는 손해가 없는 걸 전제하는 손해보다 훨씬 싸다. 확인은 워크숍 → [[05_워크숍_질문지]] **Q2 · Q12** · 사실 기록은 [[01_트랙_브리핑_원문]]

> [!error] 🔴 POSTECH·경북 트랙 공통 — **포스코를 건드리지 마라**
> 학교법인 포항공과대학교가 **POSCO 지분 약 4.5%**를 갖고 있고 **배당이 법인 재원**이다. 포스코의 주가·노무·안전·환경을 소재로 삼는 것은 이해충돌 직격이다.
> 철강 공정·설비·품질도 **주인이 있는 땅**이다 — 수소환원철강기술연구소·철강제어연구센터가 이미 있고 포스코엔 **포스코DX**가 있다.

> [!error] 🔴 labup = 래블업 판정은 **확신도 높음, 확정 아님**
> 근거 — ① 법인 영문명이 `Lablup Inc.` ② **래블업은 Upstage와 같은 컨소시엄이다**(국가 독자 AI 파운데이션 모델 사업에서 GPU 인프라 담당) ③ Microsoft가 공식 파트너사 목록에 있다 ④ 자사 컨퍼런스 표기가 `lab|up`.
> 🔴 **19:00 오프닝에서 30초 반증 테스트**: 파트너 로고에 **`Lablup` / `래블업` / `Backend.AI`** 중 하나가 있으면 이 줄을 그대로 쓴다. **셋 다 없으면 이 줄을 지운다.**

> [!warning] 🔴 래블업 숫자를 발표 자료에 쓰지 마라
> **2026-07-30 코스닥 상장예비심사를 통과했고 지금 공모 절차 중이다.** 이 시기 기업은 외부 자료에 실리는 미확정 수치·전망에 극도로 민감하다. **우리 리스크가 아니라 파트너 리스크라서 더 위험하다.**

### 🔴 브리핑이 적어준 예시 4개 — 그대로 받으면 Idea Creativity 25점 직격 (Upstage 트랙 한정, 2026-08-21)

> [!error] 🔴 금지 대상은 **도메인이 아니라 각도**다 — 정확히 읽을 것
> 덱 p.27 Background 원문: *"Paperwork is everywhere — **scholarship applications, housing lease contracts, part-time employment contracts, club expense receipts**."*
> **트랙 브리핑이 예시를 직접 적어줬다.** 234명이 같은 슬라이드를 봤다. 이 넷은 **다수 팀의 첫 아이디어**다.
> 그런데 이 트랙의 25점짜리 항목이 *"Is the problem discovery fresh? Does the solution stand apart from existing services?"* 다. **브리핑 예시를 각도 없이 받으면 이 25점이 그 자리에서 깎인다.**

| 금지 | 이유 |
| --- | --- |
| 🔴 **브리핑 예시 4개를 각도 변경 없이 그대로 주제로 쓰기** — "장학금 서류 자동 정리", "임대차계약서 요약해주는 서비스", "근로계약서 독소조항 찾아주기", "동아리 영수증 정산 앱" | **예시로 적혀 있었다 = 여러 팀이 낸다.** 문제 발굴의 신선도가 0이 된다 |

⚠️ **「예시 도메인을 쓰지 마라」가 아니다.** 도메인 자체는 금지가 아니고, 브리핑도 *"Any domain is welcome"*이라고 적었다. 금지되는 것은 **예시를 그대로 받아 각도를 안 바꾸는 것**이다.

**그 도메인을 쓰려면 셋 중 하나를 만족해야 한다**

1. 브리핑에 **없는 각도**로 비튼다 — 문서 종류가 아니라 *누가 언제 무엇 때문에 막히는가*를 바꾼다
2. *"understanding"* 이 아니라 *"getting the work done"* 의 **뒷단**을 잡는다 — 제출·마감·이의제기·상대방과의 왕복처럼 **문서를 읽은 다음에 벌어지는 일**
3. `/jx-common`([[11_평범한답]])을 **확산 전에** 돌려 다른 팀이 낼 답 목록을 먼저 뽑고, **그 목록과 겹치면 버린다** — 버린 것은 위 「버린 아이디어」 표에 적는다

🔴 **이 항목이 바꾸는 건 실행 순서가 아니라 출력 A의 1순위다.** Upstage 트랙이면 **평범한 답이 브리핑 슬라이드에 이미 인쇄돼 있다** — `/jx-common` 출력 A에 이 넷이 없으면 **손으로 넣는다.** 단 C(그래도 맞을 수 있는 것)를 먼저 확인한다 → [[02_커맨드_사전]] · [[11_평범한답]]

> [!error] 🔴 2026-08-21 2차 정정 — 여기 있던 *"22:00 창구에서 가장 먼저 돌린다"*는 틀렸다
> 이 자리엔 *"이 항목 때문에 `/jx-common`의 **실행 순서가 바뀐다.** 원래 「`/jx-track`과 동시」였는데 … 22:00 창구에서 가장 먼저 돌린다"*라고 적혀 있었다.
> **실행은 그대로 `/jx-track`과 동시다.** ① 두 커맨드는 서로 입력 의존이 없어 **동시 실행 비용이 0**이라 「먼저」가 얻는 게 없다 ② 22:00의 첫 3분은 `/jx-harvest`가 쓴다 ③ 이 항목이 정말 노린 것(예시 4개 선점)은 **출력 A의 1순위를 못박는 것**으로 이미 달성된다.
> 🔴 **게다가 이 문장은 [[02_커맨드_사전]]을 근거로 지목했는데, 그 문서는 세 곳에서 「동시」라고 적고 있다.** [[01_미션1_작전]]·[[JUNCTION-MOC]]도 같다 — **틀린 쪽은 이 파일이었다.**

### 시간 구조에서 나오는 것

| 금지                              | 이유                                                                                                                          |
| ------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 🔴 **8/22 00:00까지 방향을 못 정하는 것** | 트랙 발표(20:00–21:00)와 파트너 워크숍(21:00–22:00)을 빼면 **22:00–24:00, 실질 2시간**이 전부다. 완벽한 아이디어보다 **정해진 아이디어**가 낫다 → [[02_팀과_제약]] 하드룰 5 |
| **8/23 08:00 이후에 새 기능 붙이기**     | 그때부터는 데모 준비 시간이다. 여기서 손대면 Expo에서 터진다                                                                                        |
| **미션 제출을 "나중에 몰아서"**            | 마감이 3번인 대회다. 미션을 건너뛰면 그 자체가 감점 위험 `[확인필요]`                                                                                  |
| **Expo 3시간을 즉흥으로 버티기**          | 같은 설명을 수십 번 한다. 대본 없이 3시간이면 후반엔 말이 무너진다                                                                                     |
|                                 |                                                                                                                             |

### 🔴 AI 활용이 권장인 대회라서 생기는 것

이 대회는 AI 활용을 권장하고, **팀 4명 중 3명(기획·개발2)이 Claude Max를 쓴다.** 그런데 그건 다른 팀도 마찬가지다.

> 아래에서 **"3명"은 Claude Max 사용자 수**, **"4명·넷"은 팀 전체**다. 컨텍스트 공유 규약은 **디자이너 포함 4명 전원**에게 적용된다.

| 금지 | 이유 |
| --- | --- |
| 🔴 **"AI로 만들었다"를 차별점으로 내세우기** | **전원이 AI를 쓰는 대회다.** AI를 썼다는 건 기본값이지 자랑이 아니다. 차별점은 *무엇을 만들었나*에서 나온다 |
| 🔴 **팀원이 각자 다른 컨텍스트로 AI를 돌리기** | 넷이 따로 돌리면 산출물이 조용히 어긋난다. **`00_고정/` 4개를 네 사람이 같은 내용으로** 넣는다 → 아래 「팀 AI 규약」 |
| **AI가 뱉은 걸 검증 없이 산출물에 붙이기** | 특히 숫자·출처·기술 스펙. Expo 질의응답에서 그대로 깨진다 |
| **AI에게 "좋은 아이디어 10개" 시키고 고르기** | 확산만 하고 수렴이 없다. 방향은 [[03_회의론자]]로 **깎아서** 정한다 |

### 팀 AI 규약 — 세 사람이 같은 곳을 보게 하는 최소 규칙

1. **`00_고정/` 4개 파일은 매 호출마다 전원이 동일하게 넣는다.** 이게 팀의 공유 기억이다 → [[04_사전결정]] 포함
   ✅ **공유 수단은 GitHub 확정**(2026-08-18). 볼트 레포를 클론하면 **대회 중 갱신이 자동 반영된다** — 노션이었다면 수동 복사라 갱신이 안 갔다
2. **버린 아이디어는 즉시 이 파일에 적는다.** 안 적으면 다른 팀원의 AI가 그걸 또 제안한다
3. **한 사람이 한 역할.** 같은 산출물을 둘이 동시에 AI로 고치지 않는다 — 머지 충돌이 문서에서도 일어난다
4. 기획(나)이 `20_산출물/`의 **단일 소유자**다. 개발·디자인의 입력은 받아 적되 파일은 한 사람이 쓴다

### 대회 규정 — `[확인필요]`, 8/21 개회식에서 확인

- [ ] 사전 개발 코드 반입 규정 ← **가장 먼저 확인.** 통상 금지지만 대회마다 다르다
- [ ] 오픈소스·외부 API 사용 제한
- [ ] 데이터 사용 제약 (개인정보, 라이선스)
- [ ] 파트너 API·크레딧 사용이 **필수인지 선택인지** — 🟢 **Upstage: 필수로 확정 (2026-08-21)** *"Upstage Studio must power the core document-processing stages."* 🔴 나머지 4트랙 `[확인필요]` → [[01_심사기준]] · [[02_팀과_제약]] 「크레딧 예산」
- [ ] 제출물에 AI 생성물 표기 의무가 있는지

## 표현 금지

산출물에 쓰면 안 되는 어법. 발표 스크립트에서 특히.

- "혁신적인", "획기적인" — 형용사는 근거를 대신하지 못한다
- "~할 수 있습니다" (미래형 가능성) → **"~합니다"** (지금 되는 것만)
- "향후 확장 가능" — 안 만든 걸 만든 것처럼 들리게 하는 어법. 질문 받으면 무너진다
- 데모에 없는 기능을 있는 것처럼 말하기 → **Expo 3시간 동안 반드시 들킨다.** 부스에 오는 사람 중엔 48시간 직접 만든 개발자가 섞여 있다

- **"사용자가 불편해합니다"로 문제를 세우기** → **"이 회사는 지금 이걸 이렇게 처리하고 있습니다."** 파트너가 말한 현재 처리 방식을 그대로 인용한다. 비용·시간 숫자는 **파트너가 말한 것만**
- **"저희 팀이 관심 있는 주제라서"** — 질의응답에서 이 말이 나오면 트랙 적합성이 그 자리에서 깎인다. 이유는 항상 파트너 쪽에서 댄다
- 🔴 **덱에 없는 파트너 제품·수치를 파트너 앞에서 사실처럼 말하기** (예: AI Space · File Search · 출시 시점) → 상대가 자기 제품 담당자다. 모르면 **"덱에서 못 봤는데 맞나요"**로 묻는다

## 관련

- [[JUNCTION-MOC]] · [[02_팀과_제약]] · [[03_회의론자]]
